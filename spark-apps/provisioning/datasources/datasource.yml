apiVersion: 1

datasources:
  - name: grafana-postgresql-datasource
    type: postgres
    access: proxy
    url: postgres:5432
    database: crypto_db
    user: postgres
    secureJsonData:
      password: your_password
    isDefault: true
    editable: true
    jsonData:
      sslmode: disable
